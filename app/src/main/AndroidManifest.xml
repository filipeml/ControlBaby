<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projeto.teste">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.AdicionarBebesActivity"></activity>
        <activity android:name=".activity.AdicionarVacinasActivity"></activity>
        <activity android:name=".activity.AdicionarAlimentacaoActivity"></activity>
        <activity android:name=".activity.AdicionarCrescimentoActivity"></activity>
        <activity android:name=".activity.AdicionarPediatraActivity"></activity>
        <activity android:name=".activity.AdicionarRemedioActivity"></activity>
        <activity android:name=".activity.AdicionarFraldaActivity"></activity>
        <activity
            android:name=".MenuPrincActivity"
            android:label="@string/title_activity_menu_princ"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.PrincipalActivity"
            android:label="@string/title_activity_principal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.CadastroActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>